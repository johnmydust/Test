def format_timestamp(timestamp):
    if not timestamp:
        return "Never"
    try:
        # Format date for Excel (DD/MM/YYYY)
        dt = datetime.fromisoformat(timestamp.replace('Z', '+00:00'))
        return dt.strftime("%d/%m/%Y")  # Excel-friendly format
    except ValueError:
        return timestamp
